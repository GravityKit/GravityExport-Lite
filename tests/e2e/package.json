{
  "name": "gravityexport-lite-e2e",
  "private": true,
  "description": "End-to-end tests for the analytics consent flow and outbound link tagging.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
