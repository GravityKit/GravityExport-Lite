{
  "$comment": "Product-independent half of the GravityKit measurement contract. Events, super-props, platform enums, identity, consent, transport, scrub, and the link SCHEME rules. A product fragment supplies its own slug, destinations, cta_ids and product-specific props/enums; the generator merges core + one fragment into a single locally-namespaced Schema class.",
  "schema_version": 1,
  "events": [
    "product_activated",
    "product_deactivated",
    "gf_form_connected",
    "analytics_opt_in",
    "object_created",
    "object_configured",
    "object_published",
    "object_embedded",
    "object_rendered",
    "feature_enabled",
    "job_started",
    "onboarding_started",
    "onboarding_step_viewed",
    "onboarding_completed",
    "onboarding_dismissed",
    "onboarding_task_completed",
    "sample_created",
    "cta_clicked",
    "video_played",
    "inline_edit_saved",
    "card_moved",
    "card_voted",
    "export_completed",
    "import_completed",
    "bulk_action_completed",
    "revision_recorded",
    "revision_history_viewed",
    "revision_restored",
    "migration_export_completed",
    "migration_import_completed"
  ],
  "super_props": {
    "environment": {
      "type": "enum",
      "enum": "environment"
    },
    "site_id": {
      "type": "hash"
    },
    "gk_analytics_schema_version": {
      "type": "int"
    },
    "wp_version": {
      "type": "string"
    },
    "gf_version": {
      "type": "string",
      "nullable": true
    },
    "php_version": {
      "type": "string",
      "$comment": "major.minor only, never the patch"
    },
    "locale": {
      "type": "string"
    },
    "is_multisite": {
      "type": "bool"
    },
    "license_tier": {
      "type": "enum",
      "enum": "license_tier"
    },
    "user_role": {
      "type": "string",
      "$comment": "role NAME only"
    },
    "gk_product": {
      "type": "enum",
      "enum": "gk_product"
    },
    "gk_product_version": {
      "type": "string"
    },
    "trigger_source": {
      "type": "enum",
      "enum": "trigger_source"
    },
    "activated_product": {
      "type": "enum",
      "enum": "gk_product",
      "nullable": true
    },
    "is_activation_event": {
      "type": "bool"
    },
    "funnel_stage": {
      "type": "enum",
      "enum": "funnel_stage"
    },
    "install_channel": {
      "type": "enum",
      "enum": "install_channel",
      "$comment": "Distinguishes the wordpress.org population from the gravitykit.com-distributed one. Without this the opt-in rate is computed against the wrong denominator."
    },
    "mysql_version": {
      "type": "string",
      "$comment": "major.minor only, never the full build string"
    },
    "theme": {
      "type": "enum",
      "enum": "theme"
    },
    "is_block_theme": {
      "type": "bool"
    },
    "theme_is_child": {
      "type": "bool"
    },
    "plugin_count_bucket": {
      "type": "enum",
      "enum": "scale_bucket"
    }
  },
  "props": {
    "link_campaign": {
      "type": "enum",
      "enum": "link_campaign"
    }
  },
  "enums": {
    "environment": [
      "demo",
      "production"
    ],
    "license_tier": [
      "none",
      "free",
      "core",
      "pro",
      "all_access",
      "agency"
    ],
    "trigger_source": [
      "real",
      "walkthrough",
      "sample"
    ],
    "funnel_stage": [
      "installed",
      "setup",
      "activated",
      "habit"
    ],
    "install_channel": [
      "wordpress_org",
      "gravitykit_com",
      "unknown"
    ],
    "link_campaign": [
      "upgrade",
      "docs",
      "support"
    ],
    "link_medium": [
      "plugin",
      "frontend"
    ],
    "scale_bucket": [
      "0",
      "1",
      "2-5",
      "6-10",
      "11-25",
      "26-100",
      "101-1000",
      "1001-10000",
      "10001-50000",
      "50001-250000",
      "250001-1000000",
      "1000000+"
    ],
    "theme": [
      "divi",
      "hello_elementor",
      "astra",
      "avada",
      "beaver_builder",
      "buddyboss",
      "generatepress",
      "betheme",
      "enfold",
      "oceanwp",
      "kadence",
      "blocksy",
      "genesis",
      "bricks",
      "flatsome",
      "twenty",
      "other"
    ]
  },
  "identity": {
    "salt_option": "gk_analytics_site_salt",
    "hmac_algo": "sha256",
    "site_id_input": "home_url",
    "url_normalization": "strip_trailing_slash_and_scheme_case",
    "multisite_scope": "per_site",
    "refuse_on_no_salt": true
  },
  "consent": {
    "option": "gk_analytics_consent",
    "record": [
      "granted",
      "ts",
      "source",
      "schema_version",
      "disclosure_hash"
    ]
  },
  "attribution": {
    "salt_option": "gk_attribution_salt",
    "$comment": "Deliberately a SEPARATE salt from identity.salt_option. Reusing site_id as the discount token would retroactively re-identify every event an install ever sent."
  },
  "transport": {
    "host_constant": "GK_ANALYTICS_HOST",
    "host_filter": "gk/analytics/host",
    "default_host": "understand.gravitykit.com",
    "path": "/capture",
    "max_queue": 50,
    "timeout": 5
  },
  "scrub": {
    "free_text_cutoff": 200,
    "drop_key_patterns": [
      "^\\$"
    ],
    "redact_value_patterns": {
      "email": "/[\\w.+-]+@[\\w-]+\\.[\\w.-]+/",
      "url": "/https?:\\/\\/[^\\s]+/i"
    },
    "min_site_name_length": 4,
    "$comment": "Emails and URLs are redacted outright rather than masked. Easy Digital Downloads masks them (a*a@e*****e.com), which preserves length, first and last characters and the TLD; full redaction is stricter. Site-name scrubbing is taken FROM EDD, which strips the site name out of arbitrary strings before sending (EDD\\Telemetry\\Traits\\Anonymize::anonymize_site_name). min_site_name_length is a threshold, not a toggle: it guards against a site named \"A\" or \"My\" matching everywhere and destroying every string, which EDD does not guard against."
  },
  "links": {
    "host_allowlist": [
      "https://www.gravitykit.com/"
    ],
    "content_is_cta_id": true,
    "$comment": "utm_content always carries a cta_id value, so one identifier appears in PostHog (cta_id) and GA (utm_content) and the two join in aggregate. utm_source is the PRODUCT slug and lives in the product fragment. Destinations are validated against host_allowlist at generation time: the legacy gfexcel.com redirect drops the query string, so a tagged link routed through it arrives untagged with no error.",
    "utm_medium_default": "plugin"
  },
  "notes": {
    "props_incomplete": "The canonical taxonomy enumerates Events as code (30, with a mandated count assertion) but never enumerates Props::ALL or Enums::* as code -- they exist only as prose across its per-product sections. The props/enums above are therefore the GravityExport-scoped subset, complete for what Lite emits. Foundation must widen them to all 14 products when it adopts this file, and the taxonomy should gain code-form Props/Enums blocks at that time.",
    "days_since_install": "Removed from v1. The taxonomy declares it, but this product has no trustworthy install timestamp: the only date available is when analytics was first enabled, which is not the install date. Emitting that under this name would be a wrong number wearing a right one. Reinstate when a real install date exists.",
    "plugin_detection": "Cut on measurement, not taste: Bricks 0.4%, Oxygen 0.3%, SeedProd 0.3%, GP Google Sheets 0.07% were removed as noise, and the five form-platform competitors were collapsed into one boolean because individually each is under 1% and together only 3.8%. Caveat carried deliberately: those figures come from PAYING GravityView customers, and the wordpress.org Lite population may shop around more. That is why the aggregate survives at all \u2014 if it reads above ~10% on real Lite data, split it back into named booleans in a schema revision.",
    "scale_bucket": "The top buckets are sparse by construction: few installs hold a million entries, so those buckets identify more narrowly than the dense middle. That is the price of seeing real scale, and it is a deliberate trade rather than an oversight.",
    "theme_detection": "'other' is a real signal rather than a gap: with the measured head of the distribution named, an unrecognised parent means a custom or niche theme, which usually means an agency-built site.",
    "plugin_detection_maintenance": "This list is hand-maintained, which is the price of not shipping a roster, and the trade is deliberate. What it costs: you only learn about plugins you thought to ask about, and adding one takes a plugin release plus however long sites take to update. What it buys: a disclosure anyone can read, bounded cardinality, and no inventory of a customer's site leaving their server. Three things keep the cost small. The questions are strategic rather than operational ('do our users pay for GF add-ons' does not change weekly). Discovery is answered from data we already hold rather than from this list, so the list only has to MEASURE, not FIND. And plugin_count_bucket gives site complexity with no maintenance at all."
  },
  "plugin_detection": {
    "$comment": "A CLOSED, published list of yes/no questions. Detection is is_plugin_active() against these exact paths, so the site's plugin roster is never read, let alone transmitted. Grouped by the QUESTION each answers, not by vendor. Presence figures below are measured against 12,175 GravityView installs (latest roster per site, last 12 months, telemetry.wp_gv_tracking_logs); anything under ~5% was cut as noise. Adding an entry is a reviewable schema change.",
    "reviewed": "2026-08-03",
    "review_interval_months": 12,
    "review_procedure": "Do NOT refresh this list from intuition. Run the query below against the GravityView roster data, which already collects full plugin lists from ~12,000 installs, and let it choose. That population is paying customers rather than wordpress.org users, so treat it as a candidate generator, then confirm each candidate against Lite's own has_* rates before adding it. Anything under ~5% presence is noise and should be cut. Query: WITH latest AS (SELECT site_id, plugins, ROW_NUMBER() OVER (PARTITION BY site_id ORDER BY checkin_date DESC) rn FROM `monokit-475016.telemetry.wp_gv_tracking_logs` WHERE plugins IS NOT NULL AND checkin_date >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 365 DAY)) SELECT ... FROM latest WHERE rn = 1 -- then COUNTIF(plugins LIKE '%\"<slug>%') per candidate.",
    "export_alternatives": {
      "gravity_pdf": [
        "gravity-forms-pdf-extended/pdf.php",
        "gravity-pdf/pdf.php"
      ],
      "zapier": [
        "gravityformszapier/zapier.php"
      ]
    },
    "ecosystem": {
      "gravity_perks": [
        "gravityperks/gravityperks.php"
      ],
      "gravity_flow": [
        "gravityflow/gravityflow.php"
      ],
      "woocommerce": [
        "woocommerce/woocommerce.php"
      ]
    },
    "form_platform": {
      "other_form_plugin": [
        "wpforms/wpforms.php",
        "wpforms-lite/wpforms.php",
        "ninja-forms/ninja-forms.php",
        "formidable/formidable.php",
        "fluentform/fluentform.php",
        "forminator/forminator.php"
      ]
    },
    "page_builders": {
      "elementor": [
        "elementor/elementor.php"
      ],
      "wpbakery": [
        "js_composer/js_composer.php"
      ],
      "beaver_builder": [
        "beaver-builder-lite-version/fl-builder.php",
        "bb-plugin/fl-builder.php"
      ]
    },
    "family": {
      "gravityview": [
        "gravityview/gravityview.php",
        "GravityView/gravityview.php"
      ],
      "gravitycharts": [
        "gravitycharts/gravitycharts.php"
      ],
      "gravityimport": [
        "gravityimport/gravityimport.php"
      ]
    }
  },
  "theme_detection": {
    "$comment": "The PARENT theme is reported, never the child. Measured against 127,776 legacy GravityView installs: 28,923 distinct theme strings, 15,556 of them on exactly one site, and 27.6% of names containing the word 'child'. Child themes are routinely named after the agency or the client, so the child name is both the highest-cardinality field available and frequently a direct identifier. The parent collapses that tail into a handful of frameworks, which is simultaneously the more private choice and the more useful one: 'Divi Child' and 'Divi' are the same fact about the site. The list below is the measured head of that distribution.",
    "prefer": "template",
    "known": {
      "divi": [
        "Divi"
      ],
      "hello_elementor": [
        "hello-elementor"
      ],
      "astra": [
        "astra"
      ],
      "avada": [
        "Avada"
      ],
      "beaver_builder": [
        "bb-theme",
        "beaver-builder-theme"
      ],
      "buddyboss": [
        "buddyboss-theme"
      ],
      "generatepress": [
        "generatepress"
      ],
      "betheme": [
        "betheme"
      ],
      "enfold": [
        "enfold"
      ],
      "oceanwp": [
        "oceanwp"
      ],
      "kadence": [
        "kadence"
      ],
      "blocksy": [
        "blocksy"
      ],
      "genesis": [
        "genesis"
      ],
      "bricks": [
        "bricks"
      ],
      "flatsome": [
        "flatsome"
      ],
      "twenty": [
        "twentytwenty",
        "twentytwentyone",
        "twentytwentytwo",
        "twentytwentythree",
        "twentytwentyfour",
        "twentytwentyfive",
        "twentyseventeen",
        "twentynineteen",
        "twentysixteen"
      ]
    }
  },
  "privacy": {
    "$comment": "The originating IP cannot be withheld by a plugin: the TCP connection carries it regardless, and a server address frequently reverse-resolves to the site. An earlier draft sent $ip:null and $geoip_disable:true as an instruction to the sink. That was removed as theatre. It does not make the consent card's claim true \u2014 if the proxy strips the address the instruction is redundant, and if it does not then GravityKit already holds the IP and the promise is already broken, with the instruction merely stopping PostHog from ALSO keeping it. It also cost a hole in the otherwise absolute rule that every $-prefixed key is dropped. A guarantee that can be enforced was weakened to accommodate one that cannot.",
    "proxy_requirement": "understand.gravitykit.com MUST discard the connecting IP before forwarding to PostHog, MUST NOT set or forward X-Forwarded-For / X-Real-IP, and MUST NOT enable GeoIP enrichment. This is the ONLY control; there is no plugin-side half. Until it exists and is tested, the consent card's anonymity claim is unproven and should not be described as anything else."
  }
}